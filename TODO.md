# TODO

## Presets

- [ ] Support a `default` preset in `reformat.json` that overrides the default command behavior when present (i.e., `reformat <path>` uses the `default` preset automatically instead of the hardcoded rename+emojis+clean pipeline)
